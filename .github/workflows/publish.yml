name: Publish to Maven & Create GitHub Release
on:
  push:
    branches:
      - version/*
  workflow_dispatch:

jobs:
  build:
    uses: SLNE-Development/surf-workflows/.github/workflows/build-publish-release-gradle.yml@master
    with:
      modules: "surf-survival-events-base-*-all.jar;surf-survival-events-example-*-all.jar;surf-survival-events-freebuild-*-all.jar"
    secrets: inherit